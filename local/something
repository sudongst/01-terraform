terraform {
	# 테롶라폼 버전 지정
	required_version = ">= 1.0.0, < 2.0.0"
}

	# 공급자 지정
	required_providers {
		aws = {
			source = "hashicorp/aws"
			version = "~> 4.0"

provider "aws" {
	region = "ap-northeast-2"
}
